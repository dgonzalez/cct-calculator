function sum(a, b) {
  return parseInt(a) + parseInt(b)
}

function mul(a, b) {
  return parseInt(a) * parseInt(b)
}
function div(a, b) {
  return parseInt(a) / parseInt(b)
}

module.exports = {sum, mul, div}
